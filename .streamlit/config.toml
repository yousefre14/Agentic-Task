[theme]
# Force dark mode regardless of the user's OS settings
base = "dark"

# Kayfa AI Brand Colors
primaryColor = "#F5A623"       # gold
backgroundColor = "#080B18"    # bg_deep
secondaryBackgroundColor = "#0E1225" # bg_surface
textColor = "#F1F5F9"          # text
font = "sans serif"
